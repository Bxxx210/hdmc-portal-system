@{
    ViewBag.Title = "Reset Password";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="form-wrapper compact">
    <div class="form-page-header">
        <a href="@Url.Action("Index", "UserManagement")" class="btn-back">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6" /></svg>
            Back
        </a>
        <div class="form-page-title">Reset Password</div>
    </div>

    <div class="form-card">
        <div class="form-card-head">
            <div class="form-card-eyebrow">User Management</div>
            <div class="form-card-title">@ViewBag.UserId</div>
        </div>
        <div class="form-card-body">
            @if (ViewBag.ErrorMessage != null)
            {
                <div class="alert-error">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0"><circle cx="12" cy="12" r="10" /><line x1="12" y1="8" x2="12" y2="12" /><line x1="12" y1="16" x2="12.01" y2="16" /></svg>
                    @ViewBag.ErrorMessage
                </div>
            }

            <form method="post" id="resetForm">
                @Html.AntiForgeryToken()
                <input type="hidden" name="userId" value="@ViewBag.UserId" />
                <div class="form-field">
                    <label class="form-label">User ID</label>
                    <input type="text" class="form-input" value="@ViewBag.UserId" readonly />
                </div>
                <div class="form-field">
                    <label class="form-label">New Password</label>
                    <input type="password" name="password" class="form-input" placeholder="••••••••" required />
                </div>
            </form>
        </div>
        <div class="form-card-foot">
            <button type="submit" form="resetForm" class="btn-submit warning">
                Reset Password
            </button>
        </div>
    </div>
</div>
